# npm registry auth (set NPM_TOKEN env var or run `npm login`)
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# GitHub Packages registry auth (set GITHUB_TOKEN env var or run `npm login --registry https://npm.pkg.github.com`)
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

